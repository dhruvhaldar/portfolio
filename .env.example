# Security
# Password for accessing admin features (used in src/pages/api/authenticate.ts)
ADMIN_PASSWORD=changeme

# Build Configuration
# Set to 'true' to enable bundle analysis during build (used in next.config.mjs)
ANALYZE=false
